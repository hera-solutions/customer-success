# LogRocket Error Investigation Prompt — 2026-06-05

Drop the prompt below into a Claude Code session pointed at the local Hera repos to investigate the production errors LogRocket caught the week of May 29 – June 4, 2026.

## Context

- LogRocket queried: production project (`hera-solutions`), date range May 29 – June 4, 2026.
- Findings: 8 distinct errors, all on **legacy Vue2 paths** (`/da-management/dashboard`, `/daily-rostering/...`, `/messenger`, `/vehicles/...`). **Zero errors on Athena (`/ath/`)**, so this investigation only targets the `hera` repo + HERA- Jira prefix.
- Highest-impact issue is the Cognito `execute-api:Invoke` 403 affecting the Associate Management dashboard (~50+ hits across ≥4 users for the week).
- See companion LogRocket session links in the original conversation summary.

## The prompt

```
I need you to investigate the Hera legacy Vue2 codebase to find the source of several
production errors that LogRocket caught the week of May 29 – June 4, 2026.

CONTEXT — APP, REPO, AND JIRA PROJECT:
- Hera has two frontend apps:
    * Athena (Vue 3) — URLs contain `/ath/` — repo: /Users/johnjm/bitbucket/hera/athena
      — Jira prefix: HA (e.g., HA-1234)
    * Legacy Hera (Vue 2) — URLs do NOT contain `/ath/` — repo:
      /Users/johnjm/bitbucket/hera/hera — Jira prefix: HERA (e.g., HERA-1234)
- ALL errors below fired on legacy Vue2 paths (no `/ath/` in any URL). So this entire
  investigation lives in the `hera` repo, and any tickets discovered should have the
  HERA- prefix.
- I have read-only access. Do not commit or push anything. Just investigate and report.

CONTEXT — BRANCHES AND ENVIRONMENTS:
- LogRocket only records the PRODUCTION environment, so the errors below are happening
  in code that is currently live on production.
- Production = the main/master branch. Start your investigation there to identify the
  code causing each error.
- Hera runs two lower environments: MQA and DQA, each on their own branch (run
  `git branch -a` to discover the exact branch names).
- After you find the source of an error on main/master, CHECK THE MQA AND DQA BRANCHES
  to see if the issue has already been fixed there but not yet promoted to production.
  Use `git log <branch> -- <file>` and `git diff main..<branch> -- <file>` to compare.
- Every Hera commit includes a Jira ticket number — for the legacy Vue2 repo, the
  prefix is HERA-. If you find a fix in a lower environment, report the HERA- ticket
  number from that commit so I can track it through to release.

For each error below, find:
  1. The file(s) and line(s) on main/master where the error originates
  2. A plain-English explanation of what the code is doing wrong (or what's missing)
  3. Whether the fix is frontend code, backend code, AWS infra/IAM, or a config setting
  4. Whether MQA or DQA already has a fix for this — if yes, give:
       - The branch it's on
       - The commit hash
       - The HERA- Jira ticket number from the commit message
       - A short summary of what the fix does
  5. Suggested fix (one or two sentences) if no lower-environment fix exists yet
  6. Any related code you'd recommend hardening at the same time

Use a comprehensive search — grep for endpoint names, error strings, function names,
route paths, and relevant Cognito/Amplify/AppSync setup. Don't limit yourself to the
file that throws — follow the call chain to find the real cause.

---

ERROR 1 — Highest priority (50+ hits, 4+ users, all week)
Page route: /da-management/dashboard (Associate Management dashboard)
403 Forbidden — "AccessDeniedException: User ... is not authorized to perform
execute-api:Invoke" on these endpoints:
  - /dashboard/associate/issue-trends-summary
  - /dashboard/associate/infraction-summary
  - /dashboard/associate/netradyne-alert-summary

What to look for:
  - The Vue2 component(s) backing the `/da-management/dashboard` route
  - The service/store/composable that calls these summary endpoints
  - How AWS credentials/Cognito identity are attached to the request
  - Whether the request is using the authenticated or unauthenticated Cognito role
  - The IAM policy / Amplify auth config that maps the Cognito identity pool to the
    role allowed to call API Gateway
  - Any recent changes to the auth flow or API Gateway setup

---

ERROR 2 — Medium priority (15+ hits, 3 users)
Page route: /da-management/dashboard
401 Unauthorized — "Token has expired" on AppSync / GraphQL calls. User gets bounced
to the login page after idle periods instead of having the session refreshed silently.

What to look for:
  - How the AppSync/GraphQL client is configured (Amplify, Apollo, custom?)
  - Whether there is any token-refresh logic before token expiry
  - Where the 401 response is handled and what triggers the login redirect
  - The Cognito token lifetime config

---

ERROR 3 — Low priority (10+ hits, 1 user, Jun 4 only)
Page routes: /vehicles/<id>, /da-management/dashboard
403 Forbidden — "InvalidSignatureException: Signature expired" / AWS clock skew.

What to look for:
  - How AWS request signatures are generated on the frontend
  - Whether server time or local time is used for the signature timestamp

---

ERROR 4 — Investigate (15+ hits, 1 user, Jun 3)
Page route: /daily-rostering/<date>/roster
Generic NetworkError on GraphQL query/mutation. Figure out if it's a frontend retry
issue, backend availability issue, or user network.

Session: https://app.logrocket.com/kdsnjf/hera-solutions/s/6-019e8e45-6f00-7d6d-a563-fdeeae698962/0?t=1780505054533

What to look for:
  - The roster page component and the GraphQL operations it makes
  - GraphQL client error handling and retry logic
  - Whether failed mutations are retried or silently dropped

---

ERROR 5 — Medium-to-hard (5+ hits, 1 user, recurring all week)
Page route: /messenger
504 Gateway Timeout on the endpoint `get-staffs-unread-messages`.

What to look for:
  - The Lambda or backend handler for get-staffs-unread-messages
  - The DynamoDB queries it runs (table name, index used, filter expressions)
  - Whether it scans rather than queries
  - The Lambda's memory/timeout config
  - Whether the call could be batched or paginated

---

ERROR 6 — Easy fix (2 hits, 1 user, Jun 2)
Page route: /da-management/dashboard
JS Exception — "TypeError: null is not an object (evaluating 'l.Z.state.userInfo.tenant')"
Fires when auth state is cleared but app code still tries to read user info.

What to look for:
  - All call sites accessing `userInfo.tenant` or similar
  - Missing null/undefined guards on those reads
  - The Vuex store module where userInfo is defined

---

ERROR 7 — Easy fix (1 hit, 1 user, Jun 4)
Page route: /da-management/dashboard
JS Exception — "TypeError: Cannot read properties of undefined (reading 'printUserError')"

What to look for:
  - Where `printUserError` is called
  - Why the object it's called on is undefined at that point

Session: https://app.logrocket.com/kdsnjf/hera-solutions/s/6-019e8d91-8fb2-72b8-9848-80eb06b75f6f/0?t=1780534205883

---

ERROR 8 — Easy config fix (4 hits, 1 user, Jun 4)
Page route: /da-management/dashboard
Network Timeout — "timeout of 0ms exceeded".

What to look for:
  - HTTP client configuration (axios, fetch wrapper, etc.)
  - Anywhere a timeout value could resolve to 0
  - Whether there's a default timeout set globally

---

Output format:
A markdown report grouped by error, with file paths in clickable [file](path:line) format.

For each error, structure as:
  - **Production source:** what's broken on main/master and where
  - **Already fixed in lower env?** Yes/No — if yes: branch, commit hash, HERA- ticket
    number, short description of the fix
  - **Owner:** MSP (AWS/infra) or core engineering (code)
  - **Suggested action:** what to do next (e.g., "promote HERA-1234 from MQA to prod"
    or "open a new HERA- ticket for core eng to add token refresh")

End with a summary table:
  | Error | Page route | Owner | Already fixed in MQA/DQA? | HERA Ticket | Recommended action |

Use plain language — I don't have a coding background.
```

## Summary table from LogRocket (May 29 – Jun 4, 2026)

| # | Error | Type | Hits | Users | Sessions | Page route |
|---|---|---|---|---|---|---|
| 1 | 403 `AccessDeniedException` on `execute-api:Invoke` | Network | 50+ | ≥4 | 7+ | /da-management/dashboard |
| 2 | 401 "Token has expired" (AppSync/GraphQL) | Network | 15+ | 3 | 3 | /da-management/dashboard |
| 3 | 403 `InvalidSignatureException` (clock skew) | Network | 10+ | 1 | 1 | /vehicles/*, /da-management/dashboard |
| 4 | GraphQL `NetworkError` | Network | 15+ | 1 | 1 | /daily-rostering/<date>/roster |
| 5 | 504 on `get-staffs-unread-messages` | Network | 5+ | 1 | 1 | /messenger |
| 6 | TypeError on `userInfo.tenant` | JS Exception | 2 | 1 | 1 | /da-management/dashboard |
| 7 | TypeError on `printUserError` | JS Exception | 1 | 1 | 1 | /da-management/dashboard |
| 8 | Network Timeout — "timeout of 0ms exceeded" | Network | 4 | 1 | 1 | /da-management/dashboard |
